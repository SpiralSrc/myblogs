import React from "react";

export default function page({ params }: { params: { id: string } }) {
  return (
    <main className="wrapper">
      <div>
        <h1>Post</h1>
      </div>
    </main>
  );
}
